import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.CivicSavers.CivicConnect',
  appName: '\x16\x16CivicConnect',
  webDir: 'dist'
};

export default config;
